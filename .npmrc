# NPM Configuration for TOKİCheck

# Enable workspaces
workspaces=true

# Hoist dependencies
hoist=true

# Strict peer dependencies (disable for compatibility)
strict-peer-dependencies=false

# Auto install peer dependencies
auto-install-peers=true
